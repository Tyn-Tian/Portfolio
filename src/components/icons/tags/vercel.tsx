import { IconBrandVercel } from '@tabler/icons-react'
import type { SVGProps } from 'react'

export const VercelIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandVercel className={props.className} />
)
