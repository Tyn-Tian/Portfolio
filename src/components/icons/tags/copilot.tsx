import { IconBrandGithubCopilot } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const CopilotIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandGithubCopilot className={props.className} />
);
