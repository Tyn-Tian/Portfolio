import { IconBrandSupabase } from '@tabler/icons-react'
import type { SVGProps } from 'react'

export const SupabaseIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandSupabase className={props.className} />
)
