import { IconBrandLaravel } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const LaravelIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandLaravel className={props.className} />
);
