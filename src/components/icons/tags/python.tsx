import { IconBrandPython } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const PythonIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandPython className={props.className} />
);
