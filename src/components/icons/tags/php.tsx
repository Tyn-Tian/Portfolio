import { IconBrandPhp } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const PHPIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandPhp className={props.className} />
);
