import { IconBrandBootstrap } from '@tabler/icons-react'
import type { SVGProps } from 'react'

export const BootstrapIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandBootstrap className={props.className} />
)
