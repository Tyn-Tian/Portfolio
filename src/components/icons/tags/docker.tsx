import { IconBrandDocker } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const DockerIcon = (props: SVGProps<SVGSVGElement>) => (
<IconBrandDocker className={props.className} />
);
