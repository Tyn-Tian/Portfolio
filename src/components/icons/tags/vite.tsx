import { IconBrandVite } from '@tabler/icons-react'
import type { SVGProps } from 'react'

export const ViteIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandVite className={props.className} />
)
