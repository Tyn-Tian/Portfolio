import { IconBrandGitlab } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const GitlabIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandGitlab className={props.className} />
);
