import { IconBrandFlutter } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const FlutterIcon = (props: SVGProps<SVGSVGElement>) => (
    <IconBrandFlutter className={props.className} />
);
