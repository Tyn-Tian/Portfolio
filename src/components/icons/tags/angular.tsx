import { IconBrandAngular } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const AngularIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandAngular className={props.className} />
);
