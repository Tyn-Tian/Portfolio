import { IconBrandMysql } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const MySQLIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandMysql className={props.className} />
);
