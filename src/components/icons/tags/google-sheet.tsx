import { IconFileSpreadsheet } from "@tabler/icons-react";
import type { SVGProps } from "react";

export const GoogleSheetIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconFileSpreadsheet className={props.className} />
);
