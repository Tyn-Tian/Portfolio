import { IconBrandFirebase } from "@tabler/icons-react";
import { SVGProps } from "react";

export const FirebaseIcon = (props: SVGProps<SVGSVGElement>) => (
    <IconBrandFirebase className={props.className} />
);