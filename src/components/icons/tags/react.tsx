import { IconBrandReact } from '@tabler/icons-react'
import type { SVGProps } from 'react'

export const ReactIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandReact className={props.className} />
)
