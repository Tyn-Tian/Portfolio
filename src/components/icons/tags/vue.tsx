import type { SVGProps } from "react";
import { IconBrandVue } from "@tabler/icons-react";

export const VueIcon = (props: SVGProps<SVGSVGElement>) => (
  <IconBrandVue className={props.className} />
);
